{
  "name": "@hagnerd/react-rp",
  "version": "1.1.0",
  "description": "A simple utility function to make composing render-props easier, with a foward facing API that already supports React Hooks.",
  "main": "dist/bundle.cjs.js",
  "module": "dist/bundle.esm.js",
  "license": "MIT",
  "devDependencies": {
    "husky": "^1.2.0",
    "jest": "^23.6.0",
    "rollup": "^0.67.3"
  },
  "scripts": {
    "build": "rollup -c",
    "test:watch": "jest --watchAll",
    "test": "jest"
  },
  "husky": {
    "hoosk": {
      "pre-commit": "npm test",
      "pre-push": "npm test"
    }
  }
}
